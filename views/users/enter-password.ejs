<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unlock Post - Thoughts</title>
    <style>
        /* General body styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #3e1f47; /* Dark background color */
            color: #e0e0e0; /* Light text color */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Container for the unlock form */
        .unlock-container {
            background-color: rgba(255, 255, 255, 0.1); /* Slightly transparent background */
            padding: 30px;
            border-radius: 8px;
            max-width: 400px; /* Set max width for the container */
            width: 100%; /* Full width for smaller screens */
            text-align: center; /* Center text */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); /* Subtle shadow for depth */
        }

        /* Title styling */
        h1 {
            color: #f8bbd0; /* Title color */
            font-size: 1.8em; /* Font size */
            margin-bottom: 20px; /* Margin below the title */
        }

        /* Input field styling */
        input[type="password"] {
            width: 100%; /* Full width */
            padding: 10px; /* Padding */
            margin-bottom: 20px; /* Space below input */
            border-radius: 8px; /* Rounded corners */
            border: 1px solid #ce93d8; /* Border color */
            color: #4a148c; /* Text color */
            background-color: rgba(255, 255, 255, 0.2); /* Light background for input */
        }

        /* Button styling */
        button {
            padding: 12px 20px; /* Button padding */
            font-size: 1em; /* Font size */
            color: #ffffff; /* Text color */
            background: linear-gradient(135deg, #ab47bc, #8e24aa); /* Button background */
            border: none; /* No border */
            border-radius: 30px; /* Rounded button */
            cursor: pointer; /* Pointer cursor */
            transition: all 0.3s ease; /* Smooth transition */
        }

        button:hover {
            background: linear-gradient(135deg, #ce93d8, #ba68c8); /* Lighter background on hover */
            transform: translateY(-3px); /* Slight lift effect */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4); /* Shadow on hover */
        }
    </style>
</head>
<body>

    <div class="unlock-container">
        <h1>Unlock Post</h1>
        <form action="/unlock-post" method="POST">
            <input type="hidden" name="postId" value="<%= postId %>">
            <input type="password" name="password" placeholder="Enter password to view this post" required>
            <button type="submit">Unlock Post</button>
        </form>
    </div>

</body>
</html>
