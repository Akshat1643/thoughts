<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Thought - Thoughts App</title>
    <style>
        /* General styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #2e1b34;
            color: #f5e6fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Container styling */
        .edit-container {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 10px;
            max-width: 500px;
            width: 100%;
            text-align: center;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        /* Header styling */
        h1 {
            color: #f8bbd0;
            font-size: 1.8em;
            margin-bottom: 20px;
        }

        /* Input styling */
        .input-field {
            width: 100%;
            padding: 12px;
            font-size: 1em;
            color: #2e1b34;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            outline: none;
        }

        /* Textarea styling */
        .textarea-field {
            width: 100%;
            padding: 12px;
            font-size: 1em;
            color: #2e1b34;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            height: 150px;
            resize: none;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            outline: none;
        }

        /* Button styling */
        .save-button {
            padding: 12px 25px;
            font-size: 1em;
            color: #ffffff;
            background-color: #ab47bc;
            border: none;
            border-radius: 30px;
            text-decoration: none;
            margin-top: 20px;
            transition: all 0.3s ease;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
            cursor: pointer;
        }

        .save-button:hover {
            background-color: #ba68c8;
            transform: translateY(-3px);
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.4);
        }
    </style>
</head>
<body>

    <div class="edit-container">
        <h1>Edit Your Thought</h1>
        <!-- Edit form -->
        <form action="/edit/<%= post._id %>" method="POST">
            <input type="text" name="title" class="input-field" placeholder="New Title" value="<%= post.title %>" required>
            <textarea name="content" class="textarea-field" placeholder="New Content" required><%= post.content %></textarea>
            <button type="submit" class="save-button">Save Changes</button>
        </form>
    </div>

</body>
</html>
