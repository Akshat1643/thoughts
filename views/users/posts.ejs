<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Thought - Thoughts App</title>
    <style>
        /* General styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #2e1b34;
            color: #f5e6fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Container styling */
        .post-container {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            max-width: 500px;
            width: 100%;
            text-align: center;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        /* Header styling */
        h1 {
            color: #f8bbd0;
            font-size: 1.8em;
            margin: 0 0 10px;
        }

        /* Username display with creative style */
        .username {
            font-size: 1.2em;
            color: #ffd1dc;
            font-weight: bold;
            margin-bottom: 15px;
            background-color: rgba(255, 215, 235, 0.1);
            padding: 8px;
            border-radius: 8px;
            display: inline-block;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        /* Content styling */
        .post-content {
            font-size: 1.1em;
            color: #e9d0f3;
            line-height: 1.6;
            margin: 10px 0;
        }

        /* Button styling */
        .action-button {
            display: inline-block;
            padding: 12px 25px;
            font-size: 1em;
            color: #ffffff;
            background-color: #ab47bc;
            border: none;
            border-radius: 30px;
            text-decoration: none;
            margin: 10px 5px;
            transition: all 0.3s ease;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
            cursor: pointer;
        }

        .action-button:hover {
            background-color: #ba68c8;
            transform: translateY(-3px);
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.4);
        }
    </style>
</head>
<body>

    <div class="post-container">
        <h1>Your Thought</h1>
        <p class="username">Thought by: <%= user.name %></p>
        <div class="post-content">
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
        </div>
        <!-- Buttons for navigation -->
        <a href="/main" class="action-button">New Thoughts? Click Here</a>
        <a href="/edit/<%= post._id %>" class="action-button">Edit Thought</a>
        <a href="/delete/<%= post._id %>" class="action-button">Delete Thought</a>
    </div>

</body>
</html>
